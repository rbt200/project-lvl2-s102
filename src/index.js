import fs from 'fs';
import _ from 'lodash';

export default (first, second) => 
const res = {
    host: hexlet.io
  + timeout: 20
  - timeout: 50
  - proxy: 123.234.53.22
  + verbose: true
}
return res;
};
